"use client"
import { useState } from "react"
import Link from "next/link"
import { ArrowRight, Search } from "lucide-react"
import { posts } from "@/lib/site-data"
import { Input } from "@/components/ui/input"
import { Badge } from "@/components/ui/badge"
export function BlogExplorer(){const[q,setQ]=useState("");const filtered=posts.filter(p=>(p.title+p.excerpt+p.category).toLowerCase().includes(q.toLowerCase()));return <><div className="mb-10 flex items-center gap-3 rounded-xl border border-border bg-card px-4"><Search className="size-5 text-muted-foreground"/><label className="sr-only" htmlFor="blog-search">Search insights</label><Input id="blog-search" className="h-12 border-0 bg-transparent shadow-none focus-visible:ring-0" placeholder="Search AI, cloud, product, transformation..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map((post,i)=><article key={post.slug} className="group flex flex-col rounded-2xl border border-border bg-card p-6 transition-all hover:-translate-y-1 hover:shadow-xl hover:shadow-primary/5"><div className="flex items-center justify-between"><Badge variant="secondary">{post.category}</Badge><span className="text-xs text-muted-foreground">{post.read}</span></div><h2 className="mt-7 text-xl font-semibold leading-snug">{post.title}</h2><p className="mt-4 flex-1 text-sm leading-relaxed text-muted-foreground">{post.excerpt}</p><div className="mt-7 flex items-center justify-between border-t border-border pt-5"><span className="text-xs text-muted-foreground">{post.date}</span><Link href={`/blog/${post.slug}`} className="flex items-center gap-1 text-sm font-medium text-primary">Read <ArrowRight className="size-4"/></Link></div></article>)}</div>{filtered.length===0&&<div className="rounded-2xl border border-dashed border-border p-12 text-center"><p className="font-semibold">No articles found</p><p className="mt-2 text-sm text-muted-foreground">Try a broader search term.</p></div>}</>}

import type { Metadata } from "next"
import Link from "next/link"
import { notFound } from "next/navigation"
import { ArrowLeft, Clock3 } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { CtaSection } from "@/components/page-sections"
import { posts } from "@/lib/site-data"
export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const post=posts.find(p=>p.slug===slug);if(!post)return{};return{title:post.title,description:post.excerpt,alternates:{canonical:`/blog/${slug}`}}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const post=posts.find(p=>p.slug===slug);if(!post)notFound();return <><article><header className="border-b border-border bg-muted/30"><div className="container-site max-w-4xl py-20"><Link href="/blog" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4"/>All insights</Link><div className="mt-10 flex flex-wrap items-center gap-3"><Badge>{post.category}</Badge><span className="flex items-center gap-1.5 text-xs text-muted-foreground"><Clock3 className="size-3.5"/>{post.read}</span></div><h1 className="mt-6 text-balance text-4xl font-semibold tracking-tight sm:text-6xl">{post.title}</h1><p className="mt-6 max-w-2xl text-xl leading-relaxed text-muted-foreground">{post.excerpt}</p><p className="mt-8 text-sm text-muted-foreground">Wolfitpark Editorial · {post.date}</p></div></header><div className="container-site max-w-3xl py-16"><div className="flex flex-col gap-7 text-base leading-8 text-muted-foreground"><p className="text-xl leading-8 text-foreground">The strongest technology initiatives begin by defining the operational behavior that should change—not by selecting a tool.</p><p>Teams often feel pressure to automate quickly. But speed without context can reinforce the very friction the system was meant to remove. A better approach begins with the moments where people wait, repeat work, reconcile conflicting information, or make decisions without sufficient context.</p><h2 className="mt-5 text-3xl font-semibold text-foreground">Start with the decision, not the interface</h2><p>Map the decision owner, the information needed, the acceptable level of uncertainty, and the consequence of getting it wrong. This creates a practical boundary for software and automation. It also reveals where human judgment remains essential.</p><blockquote className="border-l-2 border-primary pl-6 text-xl leading-8 text-foreground">A useful system does not simply move faster. It makes the right work easier to recognize and complete.</blockquote><h2 className="mt-5 text-3xl font-semibold text-foreground">Design for trust in layers</h2><p>Trust grows when users can understand what happened, correct a result, and know when a system needs help. Clear permissions, visible sources, review points, and operational monitoring are product features—not technical footnotes.</p><ul className="flex list-disc flex-col gap-3 pl-6"><li>Define a narrow, measurable outcome.</li><li>Use the minimum data required to create value.</li><li>Keep important exceptions visible to the right people.</li><li>Measure quality, adoption, cycle time, and business impact together.</li></ul><h2 className="mt-5 text-3xl font-semibold text-foreground">Scale what earns the right to scale</h2><p>Release to a focused group, observe real behavior, and improve the operating model before expanding. This creates evidence, protects trust, and turns transformation into a repeatable capability instead of a one-time launch.</p></div></div></article><CtaSection title="Turn this idea into an operating advantage." description="Talk with a Wolfitpark specialist about the system, workflow, or product you are ready to improve."/></>}

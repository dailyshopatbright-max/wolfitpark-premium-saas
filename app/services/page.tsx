import type { Metadata } from "next"
import Link from "next/link"
import { ArrowRight } from "lucide-react"
import { Button } from "@/components/ui/button"
import { CtaSection, IconCard, PageHero, SectionHeading } from "@/components/page-sections"
import { processSteps, services } from "@/lib/site-data"
export const metadata:Metadata={title:"Software & AI Services",description:"Custom software, business systems, AI automation, cloud, data, and product design services from Wolfitpark.",alternates:{canonical:"/services"}}
export default function ServicesPage(){return <><PageHero eyebrow="Services" title="Technology built around the way you create value." description="From focused product delivery to enterprise transformation, our senior teams connect strategy, design, software, data, and AI." actions={<><Button size="lg" render={<Link href="/consultation"/>}>Plan your project<ArrowRight data-icon="inline-end"/></Button><Button variant="outline" size="lg" render={<Link href="/portfolio"/>}>View our work</Button></>}/><section className="section-pad"><div className="container-site"><SectionHeading eyebrow="Full-spectrum capability" title="One partner from first decision to lasting operation."/><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{services.map(([title,description,icon])=><IconCard key={title} title={title} description={description} icon={icon}/>)}</div></div></section><section className="section-pad border-y border-border bg-muted/40"><div className="container-site"><SectionHeading eyebrow="Delivery model" title="Clarity at every stage." description="We organize work around outcomes, keep important decisions visible, and ship in useful increments."/><div className="mt-12 grid gap-6 md:grid-cols-4">{processSteps.map(([n,t,d])=><article key={n}><p className="text-sm font-semibold text-primary">{n}</p><h3 className="mt-4 text-xl font-semibold">{t}</h3><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{d}</p></article>)}</div></div></section><CtaSection title="Need a team that can see the whole system?" description="Tell us where growth is creating friction. We will recommend the clearest path forward."/></>}

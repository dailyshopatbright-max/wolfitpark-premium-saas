import type { Metadata } from "next"
import { Quote, Star } from "lucide-react"
import { CtaSection, PageHero, SectionHeading } from "@/components/page-sections"
import { projects, testimonials } from "@/lib/site-data"
export const metadata:Metadata={title:"Testimonials",description:"Hear how Wolfitpark clients turned complex workflows into connected, high-performing systems.",alternates:{canonical:"/testimonials"}}
export default function TestimonialsPage(){return <><PageHero eyebrow="Client stories" title="The best systems change how work feels." description="Our clients measure success in faster decisions, fewer handoffs, happier teams, and a stronger capacity to grow."/><section className="section-pad"><div className="container-site"><SectionHeading eyebrow="In their words" title="Trusted for the work that matters."/><div className="mt-12 grid gap-5 lg:grid-cols-3">{testimonials.map(t=><figure key={t.name} className="rounded-3xl border border-border bg-card p-8"><Quote className="size-8 text-primary"/><div className="mt-7 flex gap-1">{Array.from({length:t.rating}).map((_,i)=><Star key={i} className="size-4 fill-primary text-primary"/>)}</div><blockquote className="mt-6 text-lg leading-relaxed">“{t.quote}”</blockquote><figcaption className="mt-8 border-t border-border pt-5"><p className="font-semibold">{t.name}</p><p className="mt-1 text-sm text-muted-foreground">{t.role}</p></figcaption></figure>)}</div></div></section><section className="section-pad bg-foreground text-background"><div className="container-site"><SectionHeading eyebrow="Success snapshots" title="Measured where the business feels it."/><div className="mt-12 grid gap-4 md:grid-cols-3">{projects.slice(0,3).map(p=><article key={p.slug} className="rounded-2xl border border-background/15 bg-background/5 p-6"><p className="text-3xl font-semibold text-primary">{p.result}</p><h3 className="mt-5 font-semibold text-background">{p.title}</h3><p className="mt-3 text-sm leading-relaxed text-background/60">{p.summary}</p></article>)}</div></div></section><CtaSection title="Build an outcome worth talking about."/></>}
